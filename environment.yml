name: tabletennis
channels:
  - pytorch
  - nvidia
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
      - torch>=2.1.0
      - numpy>=1.24.0
      - pandas>=2.0.0
      - scikit-learn>=1.3.0
      - tqdm>=4.65.0
